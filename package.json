{
  "name": "prod-deployment-test",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "cronbake": "^0.4.0"
  },
  "scripts": {
    "build": "bun build --compile --target bun-linux-x64 --outfile ./dist/linux-amd64 ./index.ts && cd dist && tar -czvf ./linux-amd64.tar.gz ./linux-amd64"
  }
}